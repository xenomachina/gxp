GXP relies on the following JARs in one way or another.  A copy of the jar is
included for convenience.

ant.jar - A snapshot of the ant build tool jar (only used by tests).
See http://ant.apache.org/.

args4j-2.0.12.jar - A snapshot of the args4j command line processing library.
See https://args4j.dev.java.net/.

easymock-2.5.jar - A snapshot of the EasyMock testing tool (only used by tests).
See http://www.easymock.org/.

google-base-extra.jar - Some Google code that we haven't quite open sourced
yet in the com.google.common.base package.

guava-r05.jar - A snapshot of the Google Core
Libraries. See http://code.google.com/p/guava-libraries/.

jarjar-1.0.jar - The JarJar tool (used only for creating the release
jar). See http://code.google.com/p/jarjar/.

json.jar - A snapshot of the JSON library. See http://www.json.org/.

junit-4.8.2.jar - A snapshot of the junit testing library (used only by tests).
See http://www.junit.org/

xercesImplNoMetaInf.jar - A snapshot of the xerces XML parsing library. See
http://xerces.apache.org/xerces-j/.
